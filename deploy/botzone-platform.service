[Unit]
Description=botzone-platform web
After=network.target

[Service]
Type=simple
WorkingDirectory=%h/project/botzone-platform
EnvironmentFile=-%h/project/botzone-platform/.env
ExecStart=%h/project/botzone-platform/.venv/bin/python -m bzplat.backend.cli serve --host ${BZ_HOST:-0.0.0.0} --port ${BZ_PORT:-50380}
Restart=on-failure
RestartSec=3

[Install]
WantedBy=default.target
