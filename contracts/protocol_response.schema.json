{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PlatformHoldemResponseEnvelope",
  "description": "平台唯一现行 Holdem 响应信封。顶层只能包含 response；-1=fold，-2=all-in，0=call/check，正整数=额外投入筹码。",
  "type": "object",
  "required": ["response"],
  "additionalProperties": false,
  "properties": {
    "response": {
      "type": "integer",
      "minimum": -2,
      "description": "-1 fold / -2 all-in / 0 call-check / 正整数为 raise delta"
    }
  }
}
